[package]
name = "aks3-api"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
aks3-engine = { path = "../engine" }
s3s = { workspace = true }
async-trait = { workspace = true }
bytes = { workspace = true }
futures = { workspace = true }
tracing = { workspace = true }
# `S3Request::headers` is an `http::HeaderMap`, which put_object inspects to
# reject a request that declares aws-chunked framing without a streaming
# payload sentinel. The same types build an `S3Request` by hand in the tests
# (s3s 0.14 has no constructor).
http = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
tokio = { workspace = true }

[lints]
workspace = true
